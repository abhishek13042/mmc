[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mmc"
version = "0.1.0"
description = "Money Making Concepts (MMC) trading framework — transcripts converted to code."
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
include = ["mmc*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
